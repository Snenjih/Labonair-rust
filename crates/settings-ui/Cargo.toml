[package]
name = "labonair-settings-ui"
version = "0.1.0"
edition = "2021"

[lib]
name = "labonair_settings_ui"
path = "src/settings_ui.rs"

[dependencies]
gpui = { workspace = true }
gpui-component = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }
labonair-theme = { path = "../theme" }
labonair-ui-kit = { path = "../ui-kit" }
labonair-gpui-ext = { path = "../gpui-ext" }
labonair-notifications = { path = "../notifications" }
labonair-command-palette = { path = "../command-palette" }
labonair-workspace = { path = "../workspace" }
labonair-panel = { path = "../panel" }
labonair-backend = { path = "../backend" }
# T19-010: Settings › Hosts embeds `HostManagerView` verbatim.
labonair-hosts-ui = { path = "../hosts-ui" }
labonair-ai = { path = "../ai" }
# T19-004: the UI is generated from the typed `SettingsContent` tree via the
# layered `SettingsStore` global (T19-002/003) instead of the old
# hand-maintained `FIELDS` table.
labonair-settings = { path = "../settings" }
labonair-settings-content = { path = "../settings-content" }
# T19-008: surgical `keymap.json` edits (rebind/unbind a shortcut) reuse the
# same comment-preserving tree-sitter-json editor T19-005 built for
# `settings.json`.
labonair-settings-json = { path = "../settings-json" }

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }
