[package]
name = "labonair-secrets"
version = "0.1.0"
edition = "2021"

[dependencies]
aes-gcm = "0.10"
labonair-filesystem = { path = "../filesystem" }
rand = { version = "0.8", features = ["getrandom"] }
serde_json = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
